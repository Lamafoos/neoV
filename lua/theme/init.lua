-----------------------------------------------------------
-- Theme config
-----------------------------------------------------------

-- local catppuccin = require("catppuccin")
local github_theme = require("github-theme")

github_theme.setup({
  theme_style = "dark",
})
